{
  "name": "billing-app",
  "private": true,
  "scripts": {
    "build": "npm run build -w web",
    "dev": "npm run dev -w web",
    "lint": "npm run lint -w web",
    "check-types": "npm run check-types -w web",
    "postinstall": "prisma generate --schema=packages/db/prisma/schema.prisma"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
